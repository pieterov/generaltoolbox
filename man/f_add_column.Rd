% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/f - Add column.R
\name{f_add_column}
\alias{f_add_column}
\title{Add missing columns to a data frame.}
\usage{
f_add_column(df.input, v.col, c.fill.value = NA_character_)
}
\arguments{
\item{df.input}{A data frame to which columns may be added.}

\item{v.col}{Character vector of column names to ensure in \code{df.input}.}

\item{c.fill.value}{Value used to fill new columns (default is \code{NA_character_}).}
}
\value{
A data frame with the specified columns present; newly added columns
are filled with \code{c.fill.value}.
}
\description{
Adds one or more columns to a data frame if they are not already
present, filling them with a specified default value.
}
\details{
Existing columns in \code{df.input} are left unchanged.
Only columns from \code{v.col} that are not already present are added.
}
\examples{
df <- data.frame(a = 1:3)
f_add_column(df, c("b", "c"), 0)
# Returns data frame with columns a, b, c; b and c are filled with 0.

}
\author{
Pieter Overdevest
}
